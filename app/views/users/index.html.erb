<p> 
<% @users.each do |u| %>
   <p> User: <%= u.email %>
   Genres: (<%= u.genres.uniq.map(&:name).join(', ') %>) </p><br>
    <% end %>
</p><br>

<p>
    <%= button_to "Back", new_user_path, :method => "get" %>
</p>